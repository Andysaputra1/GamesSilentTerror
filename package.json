{
  "name": "silent-terror-workspace",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "postinstall": "npm --prefix frontend ci",
    "start": "node frontend/docker/start.mjs",
    "format": "node scripts/format.mjs && python -m black backend",
    "format:check": "node scripts/format.mjs --check && python -m black --check backend"
  }
}
